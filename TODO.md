
make sure to structure the code so that:

- we can define different agents (e.g. chord analysis, phrase analysis, reference agents, etc). let's start with a basic defautl agent
- it can get context from current selection
- for the future: we can get existing annotation and analysis
- we can define tools
- conversation history

and we use browser local data to save existing conversation history.

could you create a prototype